import React from 'react';

export enum CodepaintType {
    'jsx'
};

const codepaint = (content: string, parseTo: CodepaintType): JSX.Element => {
    const element: JSX.Element = <></>;

    return element;
};

export default codepaint;
